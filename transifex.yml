version: '>=1.0'
# Transifex configuration for GitHub Actions
# Following the approach used in pyvista-js
files:
  - source: '**/*.pot'
    translation: 'locales/%language%/LC_MESSAGES/%original_file_name%.po'
    type: PO
